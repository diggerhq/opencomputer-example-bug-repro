# Bug reproducer

An OpenComputer Serverless Agent that takes a bug report and a public
repository, clones the code, writes the smallest failing test, runs it, and
reports observed versus expected with the exact command.

Full walkthrough coming with the first verified run. Fixture and reports:
`fixture/` and `fixture/BUG-REPORTS.md`.
